# 超级马里奥（有道词典笔版）项目交接文档

> 最后更新：2026-09-23 · 当前版本：v2.1.9

## 一、项目概览

| 项 | 值 |
|---|---|
| 仓库 | https://github.com/wanfeng74/super-mario.git（分支 main） |
| 应用 | 超级马里奥 · 蘑菇王国冒险（原版 SMB 重制） |
| 平台 | 有道词典笔 falcon UI（aiot-vue-cli / QuickJS 20200705） |
| 屏幕 | 960×266 横条屏，纯触摸操作 |
| appid | 8001865309000002 |
| 玩法 | 三段触控区：左按住左移 / 中按住右移 / 右按住跳跃；右上角暂停；32 关（8 世界×4）；3 存档位自动保存（`/userdisk/database/mario_save.json`） |

## 二、目录结构

```
super-mario/
├── ui/                          # 应用工程（构建产物目录）
│   ├── package.json             # appid / version（版本单一来源之一）
│   ├── src/
│   │   ├── pages/index/index.vue   # 标题/关于/调试页 UI（含操作说明文案）
│   │   ├── services/game/engine.js # 游戏引擎（碰撞/敌人/Boss/桥/渲染）
│   │   ├── services/game/levels.js # 32 关地图数据
│   │   └── services/version.js     # APP_VERSION（与 package.json 同步）
│   └── 8001865309000002.2_X_Y.amr  # 各版本安装包产物
├── scripts/
│   ├── test-bugfix.mjs          # bug 回归测试（36/36）
│   ├── test-engine.mjs          # 引擎测试（50/4，见已知问题）
│   ├── render_frame.mjs         # 无头渲染调试帧（png）
│   ├── panet-mock.mjs / panet-resolver.mjs / preload.mjs  # 测试环境 mocks
│   ├── sprites_gen.py / map_analyze.py
├── CHANGELOG.md                 # 版本变更明细
├── 玩家更新公告.md               # 玩家面向的正式公告（每次发版更新）
├── player-update-notes.md       # 开发版更新说明（技术细节）
└── README.md                    # 玩法/安装/更新日志
```

## 三、构建与打包

```sh
pnpm -C ui package        # = aiot-cli -c -q -p，产物 ui/8001865309000002.2_X_Y.amr
```

- 构建中间态：`ui/.falcon_/`（app.js.bin 为 **tokenized 二进制**，中文按 **utf-16-le** 存储，grep 中文搜不到，需按 utf-16-le 编码检索）
- AMR 为 zip（app.js.bin / manifest.json / icon.png / libs/libjsapi_panet.so）
- 本地环境：Ubuntu，node 3.12 环境，aiot-cli 在 `~/.npm-global/bin/aiot-cli`（aiot-vue-cli@1.0.32）

## 四、版本号规则（硬性约定）

**每次改动发版必须递增版本号**，三处同步：

1. `ui/package.json` → `"version"`
2. `ui/src/services/version.js` → `APP_VERSION`
3. `CHANGELOG.md` → 新增版本节

提交约定：功能改动一个 commit + 版本号升级一个 commit（`release: 软件版本号升级 X -> Y`）。

## 五、测试

```sh
node scripts/test-bugfix.mjs    # 回归：36 通过 / 0 失败
node scripts/test-engine.mjs    # 引擎：50 通过 / 4 失败（已知基线，见下）
```

- 已知基线差异：test-engine 的 4 个 FAIL 为**存档模块 jsapi 真机环境差异**（mock 无法覆盖），与业务改动无关，勿当回归。

## 六、打包后必做验证

1. manifest 版本号 = 期望版本；app.js.bin md5 与 manifest `cert` 一致
2. bundle 内含最新版号、不含旧版号（证明未用旧缓存）
3. 文案类改动：按 utf-16-le 检索新文案存在、旧文案已删（见 `玩家更新公告.md` 交付记录）

## 七、版本历史

| 版本 | 日期 | 内容 |
|---|---|---|
| v2.1.5 | 09-22 | 16 种敌人换原版 SMB 像素贴图；踩踏判定 |
| v2.1.6 | 09-22 | 怪物碰撞重做、火焰棒改原版直棒、Boss 连发跳跃、绿龟壳机制、原版龟壳贴图 |
| v2.1.7 | 09-23 | 踩踏/龟壳严格原版状态机；库巴 Boss 原版重做（2x2、斧头桥、岩浆）；桥提 2 格+斧头挂高；无敌星岩浆屏蔽恢复；其他 8 项修复；调试界面加地面贴图开关 |
| v2.1.8 | 09-23 | 修复 7-4 问号块顶不到（x50→x54 避开火焰棒）；库巴贴图条纹（1.5x 小数缩放→预烘培 48x48 整数倍）；城堡关通关后自动跳关获胜（loadLevel 重置 bridge/axe/boss） |
| v2.1.9 | 09-23 | 关于页精简：删除敌人/陷阱/道具长列表，新增三区触控操作说明 |

## 八、已知问题与待办

- **Boss 关后空白段过长**：用户曾想加小怪/烈焰棒填充，后明确"先不管"，**暂缓待用户确认**（所有 Boss 关都有空白段）。
- **火焰棒中心（SPR_FIREBAR_HUB）**：同为 1.5x 小数缩放绘制，理论上可能有轻微条纹，仅 1 格大小且旋转快不明显，未修；如用户再提 Boss 条纹类问题可一并处理。
- **test-engine 4 个 FAIL**：存档 jsapi 真机基线差异（见第五节）。
- 旧安装包 v2.1.3/v2.1.5 产物已不保留（README 安装示例里还写着 2_1_3，可顺手更新）。

## 九、GitHub 推送机制（重要）

- 本机 `~/.git-credentials` 只有 vortex 代理账号（与 GitHub 无关）；env `GITHUB_TOKEN` 无效。
- **成功方式**：token 内联、一次性、用完不落盘：
  ```sh
  git push "https://x-access-token:<TOKEN>@github.com/wanfeng74/super-mario.git" main
  ```
- Bash 里 `${1}` 占位不传参会得到空 token → `Invalid username or token`，必须内联真实 token。
- ⚠️ 用户提供的 token（ghp_ 开头）已多次明文暴露在对话中，**建议提醒用户撤销后重新生成**；若推送被拒，向用户索要新 token。

## 十、发版交付流程（每次发版照此执行）

1. 改代码（含回归测试补充）→ 递增版本号（package.json / version.js / CHANGELOG）
2. `pnpm -C ui package` 打包 → 按第六节验证
3. 上传获取交付链接（aka.doubaocdn.com）→ 交付 AMR 给用户
4. 更新 `玩家更新公告.md`（正式版措辞：尊敬的用户…一、二、三…感谢支持）+ README 更新日志
5. 功能 commit + release commit + docs commit → 按第九节推送 → 给用户文字版公告

## 十一、常用命令速查

```sh
pnpm -C ui package                          # 打包
node scripts/test-bugfix.mjs                # bug 回归
node scripts/test-engine.mjs                # 引擎测试
python3 -c "import zipfile;print(zipfile.ZipFile('ui/8001865309000002.2_1_9.amr').read('manifest.json').decode())"  # 查 manifest
```
