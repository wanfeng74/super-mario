# 超级马里奥（有道词典笔版）更新公告

**版本：v2.1.11（2026年9月23日）**

尊敬的玩家：

本次更新依据《词典笔 OS 开发文档》性能章节，对游戏渲染引擎进行了系统性优化，旨在提升词典笔上的运行流畅度，尤其改善城堡关卡等画面负载较高场景的表现。所有优化均保留降级路径，确保不同设备上画面显示始终正确。

## 一、城堡关卡背景渲染优化

- 城堡关的黑砖墙与熔岩带背景，原先每帧需执行数十次全屏及长条矩形填充，是画面开销的主要来源。
- 现已将其预渲染为周期缓存画面，每帧仅需一次整体绘制，相机移动时才重建，城堡关卡流畅度显著提升。

## 二、地面关卡背景分层缓存

- 远山、近山、云朵三层视差滚动背景，原先每帧需进行十余次贴图拼合与填充。
- 现已预渲染为周期贴图条，每帧直接平铺绘制，滚动效果与原版一致，绘制开销大幅下降。

## 三、状态栏（HUD）静态标签缓存

- 分数、金币、时间、世界、生命等静态文字标签，原先每帧反复绘制十余次文字。
- 现已预渲染为静态缓存，每帧仅更新分数、金币、时间等动态数值，文字绘制开销减半。

## 四、动态元素按列表渲染

- 岩浆、未用问号块、被顶抖动砖等动态元素，原先每帧需扫描全部数百块瓦片。
- 现已改为直接遍历各自的活动列表，每帧绘制范围精确到可见元素，整体渲染负担明显降低。

## 五、其他优化

- 消除背景色的重复全屏填充，避免无效的过度绘制。
- 标题画面静态内容（天空、地面、标题文字、角色）预渲染，每帧仅重画缓慢滚动的背景。
- 所有优化均带自动降级：设备不支持缓存时，自动回退至原逐帧绘制方式，画面显示不受影响。

## 安装方式

- 词典笔：安装 `8001865309000002.2_1_11.amr`

感谢您的支持与反馈。
